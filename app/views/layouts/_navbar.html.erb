<nav class="navbar navbar-default navbar-fixed-top">
  <div class="container-fluid">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#coll-nav" aria-expanded="false">
        <span class="fa fa-angle-down"></span>
      </button>
      <% if is_alumn? %>
        <%= link_to image_tag("Logo_Escola_X_50x50.png", :alt => "logo"), alumn_path(@current_user) %>
      <% elsif is_parent? %>
        <%= link_to image_tag("Logo_Escola_X_50x50.png", :alt => "logo"), alumns_path %>
      <% else %>
        <%= link_to image_tag("Logo_Escola_X_50x50.png", :alt => "logo"), users_path %>
      <% end %>
    </div>
    <div class="collapse navbar-collapse" id="coll-nav">
      <ul class="nav navbar-nav pull-right">
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-hospopup="true" aria-expanded="false">
            <span class="fa fa-plus"></span> Criar
          </a>
          <ul class="dropdown-menu">
            <%if is_principal? %>
              <li><%= link_to '<span class="fa fa-user-plus"></span> Responsável e Aluno'.html_safe, new_parent_path %></li>
              <li><%= link_to '<span class="fa fa-user-circle-o"></span> Secretário'.html_safe, new_secretary_path %></li>
              <li><%= link_to '<span class="fa fa-user-plus"></span> Professor'.html_safe, new_teacher_path %>
              </li>
            <% end %>
            <% if is_principal? or is_secretary? or is_teacher? %>
              <li><%= link_to '<span class="fa fa-paper-plane-o"></span> Notificação'.html_safe, new_notification_path %></li>
            <% end %>
            <% if !@current_user.is_a?(Employee) %>
              <li class="text-muted">Sem permissão</li>
            <% end %>
          </ul>
        </li>
        <li><%=link_to '<span class="fa fa-sticky-note-o"></span> Avisos'.html_safe, notifications_path %></li>
        <li><%= link_to '<span class="fa fa-search"></span> Visualizar Secretários'.html_safe, secretaries_path %></li>
        <li><%=link_to '<span class="fa fa-sign-out"></span> Sair'.html_safe, logout_path %></li>
      </ul>
    </div>
  </div>
</nav>
